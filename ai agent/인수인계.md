# OTT 이탈 방지 파이프라인 인수인계 문서

작성일: 2026-05-30  
작성자: 신통방통팀  
프로젝트: XAI 기반 OTT 신규 고객 이탈 요인 분석 및 리텐션 전략

---

## 1. 프로젝트 개요

### 목적
OTT 신규 구독자의 가입 후 21일(day0~day20) 시청 행동 데이터를 분석하여 이탈 가능성이 높은 유저를 예측하고 세그먼트별 맞춤 CRM 전략을 도출한다.

### 데이터
- 학습: 23,081명 (Membership_train.csv)
- 테스트: 2,545명 (Membership_test.csv)
- 관측 기간: 가입 후 21일 (day0~day20)
- 예측 목표: 구독 갱신 여부 (재구매=1, 이탈=0)

### 최종 모델
- 모델: CatBoost (Optuna 30 trial 튜닝 완료)
- 전체 AUC: 0.8806
- 프로모션 AUC: 0.8664
- 비프로모션 AUC: 0.8888

---

## 2. 시스템 구조

```
원본 데이터 (01_raw/)
    ↓
FastAPI 파이프라인 (step00~step10)
    ↓
캐시 저장 (fastapi/cache/)
    ↓
Dify 워크플로우 "OTT 파이프라인 실행기"
    ↓
Gemini 2.5 Pro (HTML 보고서 생성)
    ↓
HTML 보고서 저장 → localhost:8000/pipeline/report/ai-html
    ↓
다운로드 URL 출력 (Dify 결과 화면)

별도:
FastAPI /pipeline/report/json (최신 데이터 API)
    ↓
Dify 챗봇 "OTT 이탈 분석 어시스턴트" (실시간 데이터 연동)
    ↓
CRM 담당자 / 마케터 질의응답
```

---

## 3. 실행 방법

### 3-1. FastAPI 서버 실행
```powershell
cd 'C:\Users\USER\OneDrive\바탕 화면\AX-ott-churn-prediction\ai agent\fastapi'
uvicorn main:app --reload --port 8000
```

### 3-2. ngrok 실행 (Dify 연동 시 필요)
```
ngrok http 8000
```
- 고정 URL: https://gradually-compacter-superbowl.ngrok-free.dev
- FastAPI가 켜진 상태에서 별도 cmd 창에서 실행
- **주의: FastAPI 먼저 실행 후 ngrok 실행**

### 3-3. Swagger UI (API 테스트)
```
http://localhost:8000/docs
```

### 3-4. Dify 워크플로우
- URL: https://cloud.dify.ai
- 앱명: OTT 파이프라인 실행기
- 실행 후 결과는 localhost:8000/pipeline/report/ai-html 에서 확인

### 3-5. Dify 챗봇
- 앱명: OTT 이탈 분석 어시스턴트
- Dify에서 직접 사용하거나 API 키로 외부 연동 가능

---

## 4. 파이프라인 단계별 설명

| 단계 | 엔드포인트 | 역할 | force 여부 |
|---|---|---|---|
| step00 | POST /00/feature-engineering | 원본 데이터 → 피처 생성 + expanded_dataset.csv 저장 | 항상 실행 |
| step01 | POST /01/data-contract | 데이터 품질 검증 (결측, 중복, 컬럼 확인) | 항상 실행 |
| step02 | POST /02/promotion-eda | 프로모션 vs 비프로모션 재구매율 비교 | 항상 실행 |
| step03 | POST /03/2x2-eda | 4개 코호트 시청 행동 프로필 분석 | 항상 실행 |
| step04 | POST /04/baseline-comparison | LogisticRegression / XGBoost / RandomForest AUC 비교 → boosting 계열 선정 | force 선택 |
| step05 | POST /05/feature-audit | VIF / 다중공선성 / zero-inflation 감사 | 항상 실행 |
| step06 | POST /06/model-family-comparison | boosting 계열 5개 모델 AUC + 과적합 비교 → CatBoost 선정 | force 선택 |
| step07 | POST /07/tuning | Optuna 30 trial로 CatBoost 하이퍼파라미터 튜닝 | force 선택 |
| step08 | POST /08/scoring | 튜닝 모델로 OOF 예측 점수 생성 | 항상 실행 |
| step09 | POST /09/shap | SHAP + Permutation Importance 피처 중요도 계산 | 항상 실행 |
| step10 | POST /10/segmentation | S1~S6 세그먼트 배정 + 조기 개입 기준 계산 | 항상 실행 |

### force=true 사용 기준
- **최초 실행 시**: step04, 06, 07 모두 force=true
- **새 데이터 유입 시**: force=false (캐시 사용, step08~10만 재실행)
- **6개월 재학습 시**: 전체 force=true 후 run-full 실행

---

## 5. 모델 선정 과정

### step04 — 계열 선정
- 비교: LogisticRegression vs XGBoost vs RandomForest
- 기준: ROC-AUC 성능 + train_valid_gap (과적합)
- 결과: XGBoost 전 scope 1위 → boosting 계열 확정
- LogisticRegression 제외 이유: 다중공선성 문제
- RandomForest 제외 이유: gap 0.05 초과 (과적합)

### step06 — 최종 모델 선정
- 비교: LightGBM / HistGB / CatBoost / GradientBoosting / XGBoost
- 기준: gap ≤ 0.03 필터 → 통과한 모델 중 가장 많이 1등한 모델 → 통합 모델
- 결과: CatBoost 선정 (gap 0.007~0.013, 과적합 없음)
- LightGBM / HistGB 탈락 이유: gap 0.076 (과적합)

### step07 — 하이퍼파라미터 튜닝
| Scope | 튜닝 전 | 튜닝 후 | 향상 |
|---|---|---|---|
| overall | 0.8803 | 0.8806 | +0.0003 |
| promotion_only | 0.8658 | 0.8664 | +0.0006 |
| nonpromotion_only | 0.8863 | 0.8888 | +0.0025 |

---

## 6. 세그먼트 정의

### 분류 기준
- w1+w2 합계 중앙값 118분 기준으로 상위/하위군 분류
- w3 75번째 백분위 140분 기준으로 세분화

| 세그먼트 | 인원 | 재구매율 | 이탈위험 | 전략 |
|---|---|---|---|---|
| S1 | 3,801 (16.5%) | 92.5% | 7.8% | VIP 유지, 충성도 강화 |
| S2 | 4,248 (18.4%) | 76.6% | 22.4% | 3주차 시청 독려 |
| S3 | 3,525 (15.3%) | 61.2% | 38.9% | 긴급 재활성화 |
| S4 | 1,989 (8.6%) | 91.9% | 8.2% | 시청량 증가 유도 |
| S5 | 4,672 (20.2%) | 72.7% | 27.5% | 시청 습관 형성 |
| S6 | 4,846 (21.0%) | 49.7% | 50.2% | 즉각 개입, 할인 쿠폰 |

### 조기 개입 기준
- Day 7: w1 시청 27분 미만 → 11,523명 개입 필요
- Day 14: w1+w2 합계 118분 미만 → 11,507명 개입 필요
- Day 21: S1~S6 최종 배정

---

## 7. 핵심 이탈 신호 (SHAP 분석)

| 순위 | 피처 | 의미 |
|---|---|---|
| 1 | retention_w3_ratio | 3주차 리텐션 비율 |
| 2 | retention_w2_ratio | 2주차 리텐션 비율 |
| 3 | drama_ratio | 드라마 시청 비율 |
| 4 | is_promotion | 프로모션 가입 여부 |
| 5 | is_cold_start_3d | 가입 3일 내 첫 시청 여부 |
| - | payment_proxy | 결제 기기 → 이탈과 거의 무관 (최하위) |

---

## 8. 피처 정보

### 총 피처 수
- Membership_v5.csv: 91개 컬럼
- 모델 입력 피처: 77개 (식별자/타겟 제외)

### 주요 파생 피처
- watch_time(min)_w1/w2/w3: 주차별 시청 시간
- retention_w2_ratio: (w2+1)/(w1+1)
- retention_w3_ratio: (w3+1)/(w2+1)
- diff_between_w2_w1: w2 - w1 시청량 변화
- diff_between_w3_w2: w3 - w2 시청량 변화
- is_cold_start_3d: 가입 3일 내 첫 시청 여부
- is_basic/is_standard/is_premium: 멤버십 등급 (max_screen 기반)
- payment_is_mobile/pc/android/ios: 결제 기기
- age_group: 연령대 (20/30/40/50 단위)
- is_female / is_male: 성별 플래그

### VIF inf 피처 (다중공선성)
- watch_time/session w1/w2/w3, total_watch_count, diff_between_w3_w1, watch_days, active_ratio
- XGBoost/CatBoost 등 트리 기반 모델 사용 시 영향 없음

---

## 9. 파일 구조

```
ai agent/
├── fastapi/
│   ├── main.py                    # FastAPI 서버 진입점
│   ├── config.py                  # 공통 설정, 피처 목록
│   ├── cache.py                   # 캐시 저장/로드 유틸
│   ├── report.py                  # 보고서 엔드포인트
│   ├── requirements.txt           # 패키지 목록
│   ├── 00_feature_engineering/step00.py
│   ├── 01_data_contract/step01.py
│   ├── 02_promotion_eda/step02.py
│   ├── 03_2x2_eda/step03.py
│   ├── 04_model_selection/step04.py
│   ├── 05_feature_audit/step05.py
│   ├── 06_model_family_comparison/step06.py
│   ├── 07_tuning/step07.py
│   ├── 08_scoring/step08.py
│   ├── 09_shap/step09.py
│   ├── 10_segmentation/step10.py
│   └── cache/
│       ├── pipeline_state.json    # 파이프라인 완료 상태
│       ├── expanded_dataset.csv   # 모델 입력 데이터
│       ├── step04_result.json
│       ├── step06_result.json
│       ├── step06_candidates.json
│       ├── step07_result.json
│       ├── step07_best_params.json
│       ├── step08_result.json
│       ├── step08_oof.csv         # 유저별 예측 점수
│       ├── step09_shap_global.json
│       ├── step10_segment_summary.json
│       ├── step10_segment_assignment.csv
│       ├── tuned_model_overall.pkl
│       ├── tuned_model_promotion_only.pkl
│       ├── tuned_model_nonpromotion_only.pkl
│       └── ai_report.html         # Dify AI 생성 보고서
└── _data/
    ├── 01_raw/
    │   ├── Membership_train.csv
    │   ├── Membership_test.csv
    │   ├── Views_train.csv
    │   ├── Views_test.csv
    │   ├── Movies.csv
    │   └── mapping.csv
    └── 02_interim/
        └── 260513 feature/
            └── Membership_v5.csv  # 피처 엔지니어링 결과
```

---

## 10. 주요 API 엔드포인트

### 파이프라인 관리
| 메서드 | 경로 | 설명 |
|---|---|---|
| POST | /pipeline/run-full | 전체 파이프라인 비동기 실행 |
| POST | /pipeline/run-fast | 빠른 실행 (00→01→08→10) |
| GET | /pipeline/status | 파이프라인 상태 확인 |
| POST | /pipeline/reset | 캐시 전체 초기화 |

### 보고서
| 메서드 | 경로 | 설명 |
|---|---|---|
| GET | /pipeline/report | FastAPI 자체 보고서 (HTML) |
| GET | /pipeline/report/json | 파이프라인 결과 JSON (챗봇 데이터 소스) |
| GET | /pipeline/report/ai-html | Dify AI 생성 보고서 (브라우저) |
| GET | /pipeline/report/download | AI 보고서 다운로드 (.html 파일) |
| POST | /pipeline/report/save-html | Dify에서 보고서 저장 (raw text) |

---

## 11. Dify 워크플로우 (OTT 파이프라인 실행기)

### 앱명: OTT 파이프라인 실행기
- URL: https://cloud.dify.ai
- 타입: 워크플로우

### 입력 변수
| 변수명 | 기본값 | 설명 |
|---|---|---|
| mem_file | Membership_train.csv | 멤버십 데이터 파일명 |
| vh_file | Views_train.csv | 시청 이력 파일명 |
| movie_file | Movies.csv | 영화 정보 파일명 |
| mapping_file | mapping.csv | 유저 매핑 파일명 |
| force | false | 캐시 무시 여부 (true=재실행) |

### 흐름
```
시작 → step00 → step01 → step02 → step03 → step04(force) 
     → step05 → step06(force) → step07(force) → step08 
     → step09 → step10 → LLM(Gemini 2.5 Pro) 
     → HTTP(save-html) → 다운로드 URL(코드 노드) → 출력
```

### 출력 변수
| 변수명 | 설명 |
|---|---|
| download_url | 보고서 다운로드 링크 |
| body | HTTP 저장 응답 (status: OK) |

### 보고서 확인 방법
```
브라우저 보기: http://localhost:8000/pipeline/report/ai-html
파일 다운로드: http://localhost:8000/pipeline/report/download
```

### ngrok URL (고정)
```
https://gradually-compacter-superbowl.ngrok-free.dev
```

### LLM 노드 (Gemini 2.5 Pro) 주요 설정
- 출력 형식: 순수 HTML (마크다운, 이모지 금지)
- Chart.js 차트 4개 포함 (세그먼트 재구매율, 인원, 프로모션 pie, SHAP bar)
- **반드시 `<!DOCTYPE html>`, `<html>`, `<head>`, `<body>` 태그 제외하고 출력**
- `<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>` 로 시작

### HTTP 노드 (HTML 보고서 저장) 필수 설정
- URL: `https://gradually-compacter-superbowl.ngrok-free.dev/pipeline/report/save-html`
- Method: POST
- **Body 타입: raw (JSON 사용 금지 — LLM HTML의 따옴표가 JSON 파괴)**
- Body 내용: LLM 출력 변수 직접 연결

### 다운로드 URL 코드 노드 설정
- 입력 변수: 없음
- 출력 변수: `download_url` (String)
```python
def main():
    return {
        "download_url": "https://gradually-compacter-superbowl.ngrok-free.dev/pipeline/report/download"
    }
```

---

## 12. Dify 챗봇 (OTT 이탈 분석 어시스턴트)

### 앱명: OTT 이탈 분석 어시스턴트
- URL: https://cloud.dify.ai
- 타입: 채팅플로우 (Chatflow)
- 모델: Gemini 2.5 Pro (CHAT)
- 메모리: 내장 (대화 히스토리 유지)

### 용도
파이프라인 결과 기반 자유 대화형 질문/답변. FastAPI에서 실시간 데이터를 가져와 항상 최신 결과로 답변.

### 챗플로우 구조
```
시작 (사용자 질문)
  ↓
HTTP 요청 노드 (GET /pipeline/report/json → 최신 데이터)
  ↓
코드 노드 (세그먼트/SHAP/AUC 데이터 추출 및 포맷팅)
  ↓
LLM (시스템 프롬프트 + 최신 데이터 + 사용자 질문)
  ↓
답변
```

### HTTP 요청 노드 설정
- Method: GET
- URL: `https://gradually-compacter-superbowl.ngrok-free.dev/pipeline/report/json`
- Body: none

### 코드 노드 설정
- 입력 변수: `raw` → HTTP 요청 노드의 `body` 연결
- 출력 변수: `context` (String)
```python
import json

def main(raw: str) -> dict:
    data = json.loads(raw)

    step10 = data.get("step10", {})
    step09 = data.get("step09", {})
    step07 = data.get("step07", {})

    # 세그먼트
    segs = step10.get("segments", [])
    seg_text = ""
    for s in segs:
        seg_text += (
            f"{s['segment']}: {s['row_count']}명 ({round(s.get('row_share',0)*100,1)}%) "
            f"재구매율={s.get('repurchase_rate','?')} 이탈위험={s.get('mean_churn_risk','?')}\n"
        )

    # SHAP
    shap_list = step09.get("by_scope", {}).get("overall", {}).get("shap", {}).get("top20", [])
    shap_text = ""
    for i, f in enumerate(shap_list[:10], 1):
        shap_text += f"{i}. {f['feature']} ({f.get('mean_abs_shap', 0):.5f})\n"

    # AUC
    auc_text = ""
    for scope, info in step07.get("by_scope", {}).items():
        auc_text += f"{scope}: {info.get('tuned_auc', '?')}\n"

    return {
        "context": f"=== 세그먼트 현황 ===\n{seg_text}\n=== SHAP 상위 10 피처 ===\n{shap_text}\n=== 모델 AUC ===\n{auc_text}"
    }
```

### LLM 노드 USER 메시지
```
[최신 파이프라인 데이터]
{{코드.context}}

[사용자 질문]
{{시작.query}}
```

### LLM 노드 시스템 프롬프트
```
당신은 OTT 스트리밍 서비스 전문 고객 이탈 방지 AI 어시스턴트입니다.
당신의 이름은 "이탈방지 AI"이며, CRM 담당자와 마케터가 주로 사용합니다.

[배경 및 역할]
- OTT 구독자 데이터를 기반으로 이탈 예측 모델을 구축하고 세그먼트를 분석합니다
- 모델: CatBoost (boosting 계열, Optuna 튜닝 완료)
- 관측 기간: 가입 후 21일 (day0~day20)
- 예측 목표: 구독 갱신 여부 (재구매=1, 이탈=0)

[세그먼트 기준 - 고정]
- 상위군(S1~S3): w1+w2 합계 >= 118분
- 하위군(S4~S6): w1+w2 합계 < 118분
- S1/S4: w3 >= 140분 (3주차 활발)
- S2/S5: 0 < w3 < 140분 (3주차 중간)
- S3/S6: w3 = 0분 (3주차 시청 없음, 가장 위험)

[피처 설명]
- retention_w3_ratio: 3주차 리텐션 비율 (w3+1)/(w2+1)
- retention_w2_ratio: 2주차 리텐션 비율 (w2+1)/(w1+1)
- is_cold_start_3d: 가입 3일 내 첫 시청 여부
- drama_ratio: 전체 시청 중 드라마 비율
- thriller_crime_ratio: 스릴러/범죄 장르 시청 비율
- is_promotion: 프로모션 가입 여부
- payment_proxy: 결제 기기 (이탈과 거의 무관)
- w1/w2/w3: 1~3주차 총 시청 시간(분)
- age_group: 연령대 (20/30/40/50 단위)
- is_female / is_male: 성별

[세그먼트별 전략 방향 - 고정]
- S1 (최우수 고가치): VIP 혜택 제공, 충성도 강화
- S2 (고가치 중간): 3주차 시청 독려, 신규 콘텐츠 추천
- S3 (고가치 위험): 긴급 재활성화 필요, 이탈 원인 파악
- S4 (소규모 우수): S1과 유사, 시청량 증가 유도
- S5 (저가치 중간): 콘텐츠 다양성 확대, 시청 습관 형성
- S6 (최고 위험): 즉각 개입, 할인 쿠폰 또는 무료 콘텐츠 제공

[연령대별 CRM 전략 - 고정]
20대:
- 선호 장르: 드라마, 로맨스, 액션/어드벤처
- 채널: 모바일 앱 푸시, SNS 스타일의 짧고 감각적인 메시지
- 핵심 소구: 트렌디한 신작, 친구 추천 기능, 할인 혜택

30대:
- 선호 장르: 드라마, 스릴러/범죄, 다큐멘터리
- 채널: 이메일, 앱 푸시
- 핵심 소구: 바쁜 일상 속 편리한 시청, 가족 계정 혜택, 큐레이션 추천

40대:
- 선호 장르: 역사/전쟁, 드라마, 다큐멘터리
- 채널: 이메일, SMS
- 핵심 소구: 안정적인 서비스 품질, 다양한 고전/명작 콘텐츠, 가족 시청

50대 이상:
- 선호 장르: 역사/전쟁, 다큐멘터리, 드라마
- 채널: SMS, 이메일
- 핵심 소구: 쉬운 사용법, 고객 서비스 강조, 건강/교양 콘텐츠

[성별별 CRM 전략 - 고정]
여성(F):
- 선호 장르: 드라마, 로맨스, 예능/코미디
- 메시지 톤: 따뜻하고 공감적, 감성적 표현 활용
- 소구 포인트: 인물/스토리 중심 추천, 힐링 콘텐츠, 시즌 완결 알림

남성(M):
- 선호 장르: 액션/어드벤처, 스릴러/범죄, SF/판타지
- 메시지 톤: 간결하고 직접적, 정보 중심
- 소구 포인트: 장르 신작 알림, 독점 콘텐츠, 성능/화질 강조

[세그먼트 × 성별 × 연령 조합 CRM 예시]
- S6 + 20대 여성: "요즘 바빠서 못 보셨죠? 딱 당신 취향의 드라마 골라뒀어요. 지금 복귀하시면 1주일 무료!"
- S6 + 30대 남성: "아직 안 돌아오셨군요. 이번 달 스릴러 신작 3편 나왔습니다. 지금 복귀하면 30% 할인."
- S3 + 40대 여성: "잠시 바쁘셨나요? 기다리던 드라마 시즌2가 완결됐어요. 지금 이어보세요."
- S1 + 20대 남성: "OTT 충성 회원님께 드리는 특별 혜택 — 액션 신작 독점 선공개 안내드립니다."

[조기 개입 기준 - 고정]
- Day 7: w1 시청 27분 미만 → 개입 필요
- Day 14: w1+w2 합계 118분 미만 → 개입 필요
- Day 21: S1~S6 최종 배정

[답변 규칙]
1. 사용자 메시지에 포함된 [최신 파이프라인 데이터]를 기반으로 수치를 답변하세요
2. 데이터가 없거나 불러오지 못한 경우 "최신 데이터를 불러올 수 없습니다"라고 안내하세요
3. 숫자는 항상 구체적으로 제시 (비율, 인원수 포함)
4. 인과관계 주장 절대 금지 ("~때문에 이탈" X → "~와 연관된다" O)
5. CRM 메시지 요청 시 반드시 연령대와 성별을 확인하여 맞춤 메시지 생성
6. 연령/성별 정보가 없으면 "어떤 연령대와 성별 유저를 대상으로 하시나요?" 라고 먼저 질문
7. 모델 예측임을 항상 명시 (확정 아님)
8. 전문 용어는 괄호 안에 쉬운 설명 추가

[질문 유형별 처리 방식]
- "메시지 만들어줘" → 연령대·성별 확인 후 세그먼트 특성 반영한 CRM 메시지 3가지 생성
- "보고서 줘" → 전체 파이프라인 결과 자연어 요약
- "위험 유저 알려줘" → S3, S6 인원, 재구매율, 권장 전략 설명
- "모델 성능 어때" → AUC, 과적합 gap, 세그먼트별 재구매율 제시
- "~가 뭐야" → 용어 쉽게 설명
- "프로모션 vs 일반" → 두 그룹 차이 비교
- "피처가 중요해" → SHAP 기반 상위 피처 설명

[하면 안 되는 것]
- "이 유저는 반드시 이탈합니다" 같은 단정적 표현
- 데이터에 없는 수치 임의 생성
- 인과관계 단정
- 연령/성별 확인 없이 CRM 메시지 생성
```

### 가능한 질문 예시
- "S6 유저한테 보낼 CRM 메시지 만들어줘 (30대 여성)"
- "이탈 위험 높은 유저 알려줘"
- "모델 성능 어때"
- "프로모션 유저 vs 일반 유저 차이 설명해줘"
- "SHAP 결과 설명해줘"
- "retention_w3_ratio가 뭐야"

### 외부 연동 방법
Dify 챗봇은 API를 통해 외부에서도 사용 가능:
1. **Dify 공개 URL**: 게시 후 생성되는 공개 링크로 브라우저 접속
2. **Dify API**: API 키 발급 후 FastAPI /chat 엔드포인트에서 Dify API 호출하여 통합
3. **Dify Embed**: iframe 코드로 어느 웹페이지에나 삽입

---

## 13. 재학습 주기 및 운영 가이드

### 재학습 기준
- 6개월 주기: run-full로 전체 재학습
- 새 데이터 유입 시: run-fast (모델 재학습 없이 스코어링만)
- 모델 재학습 필요 신호: AUC가 0.85 이하로 떨어질 때

### 운영 순서
1. 새 데이터를 `_data/01_raw/` 폴더에 저장
2. Dify 워크플로우 "OTT 파이프라인 실행기" 실행 (파일명 입력)
3. `localhost:8000/pipeline/report/ai-html` 에서 HTML 보고서 확인
4. Dify 출력의 `download_url` 클릭하여 .html 파일 다운로드
5. S3, S6 유저 대상 CRM 캠페인 실행
6. Dify 챗봇 "OTT 이탈 분석 어시스턴트"에서 세부 질의응답

### 주의사항
- ngrok은 FastAPI 켜진 후 실행해야 함
- FastAPI 재시작 시 캐시는 유지됨 (pipeline/reset 하지 않는 한)
- step07 (Optuna 튜닝)은 30 trial × 3 scope = 약 30~60분 소요
- Dify 무료 플랜은 챗봇 앱 생성 제한 있음 (채팅 플로우 사용)
- 챗봇은 FastAPI 서버 + ngrok이 켜져있어야 실시간 데이터 조회 가능

---

## 14. 알려진 이슈

| 이슈 | 원인 | 해결방법 | 상태 |
|---|---|---|---|
| Dify HTML 보고서 깨짐 | LLM 생성 HTML의 따옴표가 JSON 파괴 | HTTP 노드 본문을 raw로 설정 | **해결완료** |
| LLM이 전체 HTML 구조 생성 | DOCTYPE/html/body 태그 포함 시 중복 구조 | 시스템 프롬프트에 해당 태그 제외 지시 추가 | 주의 필요 |
| ngrok URL 변경 | 무료 플랜 재시작 시 URL 변경 (현재는 고정됨) | 고정 도메인 확인 후 Dify 노드 URL 수정 | 고정 URL 사용 중 |
| step07 타임아웃 | Optuna 튜닝 시간 초과 | force=false로 캐시 사용 또는 N_OPTUNA_TRIALS 줄이기 | 운영 시 주의 |
| nonpromotion AUC 하락 | 테스트 샘플 수 부족 (1,252명) | 데이터 추가 수집 후 재평가 | 미해결 |
| 챗봇 미작동 | FastAPI/ngrok 꺼진 상태 | FastAPI + ngrok 실행 후 사용 | 운영 시 주의 |

---

## 15. 패키지 목록 (주요)

```
fastapi
uvicorn
pandas
numpy
scikit-learn
xgboost
lightgbm
catboost
optuna
shap
```

설치:
```
pip install -r requirements.txt
```

---

## 16. 에이전트 아키텍처 검토

### 에이전트 판단 기준 (단 하나)

> **"LLM이 다음 행동을 스스로 결정하는가?"**
>
> YES → 에이전트 / NO → 파이프라인

---

### 현재 시스템의 정확한 분류

| 컴포넌트 | 분류 | 이유 |
|---|---|---|
| Dify 워크플로우 "OTT 파이프라인 실행기" | **파이프라인** | HTTP 호출 순서가 코드에 고정, LLM은 마지막에 텍스트만 생성 |
| Dify Chatflow "OTT 이탈 분석 어시스턴트" | **파이프라인** | HTTP → Code → LLM 순서 고정, LLM이 흐름을 결정하지 않음 |
| LangGraph agent.py | **파이프라인** | `add_edge`만 사용, 흐름이 코드에 고정, 조건 분기 없음 |
| Streamlit app.py | **파이프라인** | LangGraph를 고정 순서로 호출 |

**요약: 현재 시스템은 전체가 파이프라인이며 에이전트가 없음.**

---

### 진짜 에이전트로 만드는 방법

#### 방법 A: Dify Agent 타입 — 단일 에이전트

cloud.dify.ai에서 **Workflow/Chatflow가 아닌 "Agent" 앱 타입**을 선택하면 진짜 에이전트가 됩니다.

**구현 방법:**
1. Dify → 새 앱 → **Agent** 타입 선택
2. FastAPI 엔드포인트를 커스텀 도구로 등록:
   - `GET /pipeline/report/json` → 전체 분석 결과
   - `GET /pipeline/status` → 파이프라인 상태
3. 시스템 프롬프트 작성 (도구 선택 기준 명시)
4. 완료

**왜 진짜 에이전트인가:**
```
사용자: "S6 이탈 원인 분석해줘"

LLM 판단: 세그먼트 데이터 필요 → get_segments 호출
LLM 관찰: S6 4,846명, 이탈위험 50.2%
LLM 판단: 원인을 알려면 SHAP 필요 → get_shap 호출
LLM 관찰: retention_w3_ratio 1위
LLM 판단: 충분함 → 최종 답변 생성

← 매 단계를 LLM이 결정 (ReAct 패턴)
```

**한계:** 단일 에이전트. 병렬 전문가 실행 없음.

---

#### 방법 B: LangGraph conditional edges — 멀티에이전트

agent.py에 `add_conditional_edges`를 추가하면 목표 다이어그램의 멀티에이전트 시스템이 됩니다.

**핵심 변경사항:**
```python
# 현재 (파이프라인)
g.add_edge("load", "parallel")
g.add_edge("parallel", "action")
# → 흐름이 코드에 고정

# 변경 후 (진짜 에이전트)
def route_after_judge(state) -> str:
    if state["judge_verdict"] == "sufficient" or state["iteration"] >= 2:
        return "final_report"
    return "supervisor"  # LLM 판단에 따라 재계획

g.add_conditional_edges("judge", route_after_judge, {
    "final_report": "final_report",
    "supervisor": "supervisor"   # ← 루프 발생, 진짜 에이전트
})
```

**달성 가능한 구조:**
```
Supervisor (LLM 계획 수립)
    ↓ 병렬
[Specialist 1~5] (각자 다른 시스템 프롬프트)
    ↓
Judge (LLM 품질 평가)
    ↓
조건 분기: 충분 → 보고서 / 부족 → Supervisor 재계획 (루프)
```

---

### 방법 비교

| | Dify Agent 타입 | LangGraph conditional |
|---|---|---|
| 에이전트 여부 | ✅ 단일 에이전트 | ✅ 멀티에이전트 |
| 구현 난이도 | 낮음 (1~2시간) | 중간 (1~2일) |
| 병렬 실행 | ❌ | ✅ ThreadPoolExecutor |
| Supervisor/Judge 분리 | ❌ | ✅ |
| 재계획 루프 | ✅ (ReAct 내장) | ✅ (conditional edges) |
| 목표 다이어그램 일치 | 절반 | 완전 |

---

### 중요: Chatflow 병렬 LLM은 에이전트가 아님

Chatflow에 LLM 노드를 5개 병렬로 넣어도 에이전트가 아닙니다.
- LLM이 텍스트를 생성하지만 실행 흐름을 제어하지 않음
- 그래프 구조(코드)가 5개 모두를 항상 실행
- "멀티 LLM 파이프라인"이라고 부르는 게 정확함

---

### 권장 구현 순서

1. **즉시 (1~2시간)**: Dify Agent 타입 앱 신규 생성 → 단일 에이전트 완성
2. **이후 (1~2일)**: agent.py에 `add_conditional_edges` 추가 → 멀티에이전트 완성
3. **최종**: Dify는 UI/인터페이스, LangGraph가 에이전트 로직 담당
